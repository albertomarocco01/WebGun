node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo
.lighthouse/

# Le foto del banco sono RUMORE generato: 1600x1100 di pixel casuali, ~5 MB
# l'una, incomprimibili per costruzione. Servono a rendere la pagina lenta
# davvero, e in un repo di regia sarebbero venti megabyte di niente che nessuno
# rilegge. Si rigenerano con `npm run foto`, che e' deterministico (seme fisso):
# due macchine ottengono gli stessi byte, quindi la misura resta confrontabile.
public/foto/
